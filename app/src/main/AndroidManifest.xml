<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="studios.dlamini.com.zulubasicphrases">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon144"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon144"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ZuluPhrases"></activity>
        <activity android:name=".SpanishPhrases"></activity>
        <activity android:name=".PortuguesePhrases"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>